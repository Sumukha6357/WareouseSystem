# --- DATABASE CONFIG ---
# Used by postgres and api containers
DB_USERNAME=postgres
DB_PASSWORD=warehouse_root_pass

# --- SECURITY CONFIG ---
# Must be at least 256 bits for production
JWT_SECRET=replace_with_a_long_random_string_for_production_use
JWT_EXPIRATION_MS=86400000

# --- BACKEND CONFIG ---
# Options: local, dev, prod
SPRING_PROFILES_ACTIVE=dev

# --- FRONTEND CONFIG ---
# For local dev: http://localhost:8080
# For docker dev/prod: http://api:8080
BACKEND_BASE_URL=http://api:8080

# CORS: Update to match your production domain
ALLOWED_ORIGINS=http://localhost:3000

# --- PORT CONFIG ---
# Dev Host: 3050/5050, Prod Host: 3051/5051
WEB_PORT=3051
API_PORT=5051
